// Shared Zod schemas — will be expanded in subsequent steps
// import { z } from 'zod'

// Placeholder export — requires zod to be installed in each consuming package
export const SCHEMAS_VERSION = '1.0.0'
